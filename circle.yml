dependencies:
  post:
    - go get github.com/github/hub
    - npm install -g semver

deployment:
  release:
    branch: release
    commands:
      - current=`git describe --abbrev=0 --tags`
      - next=`semver -i $current`
      - hub release create $APPNAME "v${next}"
