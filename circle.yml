dependencies:
  post:
    - go get -d github.com/laher/goxc
    - goxc -t

deployment:
  release:
    branch: release
    commands:
      - goxc -wlc default publish-github -apikey=$GITHUB_ACCESS_TOKEN
      - goxc -bc="linux,windows,darwin"
