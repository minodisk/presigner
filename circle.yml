dependencies:
  pre:
    - go get github.com/github/hub

test:
  post:
    - ls -al

deployment:
  release:
    branch: release
    commands:
      - version=`git describe --abbrev=0 --tags`
      - echo $version
      - ls -al
      # - hub release create $APPNAME $version
